# Configure the created bundle
private.packages=org.etri.slice.core.*
embed.dependency=weka-local,;scope=!provided|test
embed.directory=lib
bundle.classpath=.,{maven-dependencies}
import.packages=*;resolution:=optional